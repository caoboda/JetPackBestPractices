<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rxjava">

    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:name=".rxlifecycle.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetPackBestPractices">
        <activity android:name=".subject.SubjectActivity"/>
        <activity android:name=".map.TestMapActivity" />
        <activity android:name=".ThreadSwitchActivity" />
        <activity android:name=".MergerActivity" />
        <activity android:name=".TransFormActivity" />
        <activity android:name=".FilterActivity" />
        <activity android:name=".CreateOperatorActivity" />
        <activity android:name=".source.TestMyRxJavaActivity" />
        <activity android:name=".rxlifecycle.RxLifeCycleActivity" />
        <activity android:name=".rxlifecycle.MainActivity" />
        <activity android:name=".permission.PerMissionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>