<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.databinding">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.JetPackBestPractices">
        <activity android:name=".MainActivity"/>
        <activity android:name=".simple2.MainActivity2"/>
        <activity android:name=".simple3.MainActivity3"/>
        <activity android:name=".simple4.MainActivity4"/>
        <activity android:name=".simple5.MainActivity5"/>
        <activity android:name=".simple6.MainActivity6"/>
        <activity android:name=".simple7.MainActivity7">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>